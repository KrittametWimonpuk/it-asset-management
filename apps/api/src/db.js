// ---------------------------------------------------------------------------
// สร้าง Prisma Client เพียงตัวเดียว แล้ว export ให้ไฟล์อื่นเรียกใช้ร่วมกัน
// (ถ้าสร้างใหม่ทุกไฟล์จะเปลือง connection ฐานข้อมูล)
// ---------------------------------------------------------------------------
import { PrismaClient } from '@prisma/client'

export const prisma = new PrismaClient()
