{
  "family": "${APP_NAME}-web",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "${TASK_CPU}",
  "memory": "${TASK_MEMORY}",
  "executionRoleArn": "${EXEC_ROLE_ARN}",
  "containerDefinitions": [
    {
      "name": "web",
      "image": "${IMAGE_WEB}",
      "essential": true,
      "portMappings": [{ "containerPort": 80 }],
      "environment": [
        { "name": "API_UPSTREAM", "value": "${ALB_DNS}" },
        { "name": "API_UPSTREAM_SCHEME", "value": "https" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/${APP_NAME}-web",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "web"
        }
      }
    }
  ]
}
